language: ruby

rvm:
  - 2.4.1

branches:
  only:
    - master
    - develop

before_install:
  - gem install bundle
  - gem build project1.gemspec
  - rm Gemfile.lock

install:
  - bundle install
  - gem install project1-0.0.0.gem

script: 
  - rake test